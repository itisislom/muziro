# Log — 2026-09-14

- **#1**: Создан базовый файл [index.html](file:///d:/Projects/Programing/Apps/Muziro/index.html) с контейнером 16:9.
- **#2**: Фон `html` и `body` переведен на полностью белый (`#ffffff`).
- **#3**: Создан документ [docs/architecture.md](file:///d:/Projects/Programing/Apps/Muziro/docs/architecture.md) с описанием архитектуры и стека (Web Audio API, AudioWorklet, DSP).
- **#4**: Инициализирована структура ежедневных логов в `docs/logs/`.
- **#5**: Добавлена прямоугольная кнопка `Upload Music` (черный фон, белый текст) в левый верхний угол холста.
- **#6**: Сдвинута кнопка `Upload Music` ближе к левому краю, внедрена адаптивная система единиц (`clamp`, `aspect-ratio: 16 / 9`) для экранов ПК любого разрешения.
- **#7**: Форма кнопки `Upload Music` изменена с прямоугольной на строго квадратную (`aspect-ratio: 1 / 1`) с центрированным текстом.
- **#8**: Кнопка `Upload Music` возвращена в прямоугольный формат с аккуратно скругленными краями (`border-radius`) и текстом в одну строку.
- **#9**: Текст кнопки изменен с `Upload Music` на `Upload Track`.
- **#10**: Реализован таймлайн дорожки: при загрузке любого поддерживаемого аудиофайла через `Upload Track` файл декодируется через Web Audio API (`decodeAudioData`), отображается имя, длительность, рендерится осциллограмма (waveform) на HTML5 Canvas и активируется интерактивный плейхед с возможностью воспроизведения по клику.
- **#11**: Добавлены кнопки `Resume` и `Pause` в верхнюю панель управления: пауза фиксирует позицию плейхеда без сброса, а продолжение возобновляет трек с текущей точки.
- **#12**: Таймлайн вынесен в отдельный изолированный модуль [timeline.html](file:///d:/Projects/Programing/Apps/Muziro/timeline.html) с поддержкой мультитрековости (список треков, общая шкала времени/рулер, сквозной глобальный плейхед для синхронного воспроизведения всех дорожек) и бесшовной связкой с [index.html](file:///d:/Projects/Programing/Apps/Muziro/index.html) через `postMessage` / iframe.
- **#13**: Кнопки `Resume` и `Pause` объединены в единую транспортную кнопку в стиле FL Studio с плавной морфинг-анимацией иконки (треугольник Play плавно разделяется и превращается в две вертикальные полосы Pause) и фирменной янтарной неоновой подсветкой активного состояния; `BUG FIXED`: исправлена закрывающая скобка CSS-селектора `.btn:disabled`.
- **#14**: Полная математическая синхронизация плейхеда с аудиосэмплами вейвформы (динамический расчет координат через `getBoundingClientRect()` контейнера дорожек, исключение накопления погрешности при рендере осциллограммы, сэмплирование пиков строго по временным отрезкам шкалы), сквозное наложение плейхеда на рулер и дорожки; удалена оранжевая подсветка из кнопки Play/Pause в пользу строгого монохромного дизайна.
- **#15**: Добавлена четырехугольная (квадратная) кнопка Stop/Reset рядом с Play/Pause: по клику останавливает воспроизведение, сбрасывает плейхед и таймлайн строго в начальную позицию (00:00.0) и переводит транспортную кнопку в состояние Play.
- **#16**: Добавлено горизонтальное масштабирование таймлайна с помощью `Ctrl + Колесико мыши` (Zoom In/Out от 1.0x до 30.0x): реализована плавная привязка фокуса зума к положению курсора мыши (Cursor-Anchored Zooming), динамическая адаптация сетки делений рулера (вплоть до десятых долей секунды) и автоматическая перерисовка вейвформы дорожек без потери качества.
- **#17**: `BUG FIXED`: Устранено наложение таймлайна, вейвформы и плейхеда на названия треков при приближении (Zoom In). Архитектура таймлайна разделена на две независимые параллельные колонки (фиксированная колонка заголовков слева с собственным z-index и скролл-вьювер сетки времени справа с синхронной вертикальной прокруткой), что физически исключает вылезание аудиодорожек за левую границу сетки.
- **#18**: Добавлена возможность инлайн-переименования аудиодорожки по клику на её название: название мгновенно трансформируется в текстовое поле с автофокусом и выделением текста, сохранение изменений происходит по нажатию `Enter` или при клике вне поля (`blur`), а отмена — по клавише `Escape`.
- **#19**: По центру верхней панели управления добавлен точный цифровой секундомер/таймкод таймлайна (`MM:SS.ms`, формат `00:00.000`): оформлен в стиле DAW-индикатора с табличными моноширинными цифрами, обновляется в реальном времени с частотой кадров воспроизведения, синхронизирован с перемоткой по клику и сбрасывается в `00:00.000` при нажатии Stop/Reset.
- **#20**: Добавлено динамическое серое состояние для квадратной кнопки Stop/Reset: если плейхед находится в самом начале (00:00.000), кнопка автоматически тускнеет (серый значок квадрата `#666666`, сниженная прозрачность `0.45` и отключение hover-эффектов), а как только плейхед сдвигается вперед (воспроизведение или перемотка), кнопка загорается чистым белым цветом и становится активной.
- **#21**: Слева от названия каждой аудиодорожки добавлен индивидуальный регулятор громкости от 100 до 0%: встроен вертикальный микшерный слайдер с цифровым индикатором текущего значения, подключенный к отдельному узлу `GainNode` Web Audio API для плавного изменения уровня громкости дорожки в реальном времени.
- **#22**: Регулятор громкости дорожки переработан в горизонтальный формат (слева направо) с увеличенным удобным слайдером и интерактивным процентным бейджем: по клику на процент открывается числовое поле для точного ручного ввода значения со строгим диапазоном от 0 до 100%, сохранением по `Enter`/`blur`, отменой по `Escape` и мгновенной синхронизацией со слайдером и звуковым узлом `GainNode`.
- **#23**: Реализовано интерактивное выделение аудиодорожек в таймлайне с плавной анимацией и четкой черной обводкой: при клике на карточку заголовка или на область осциллограммы вся полоса дорожки обрамляется 2px черной рамкой (`box-shadow: inset`) с эффектом импульса (`trackSelectPulse`), боковым черным акцентным маркером и контрастной фоновой подсветкой, а клик в пустое пространство снимает выделение.
- **#24**: Выделение дорожки строго ограничено областью таймлайна (полоса осциллограммы): с левой панели заголовков (название, регулятор громкости, тайминг) полностью удалены все обводки и фоновые подсветки, чтобы черная рамка с анимацией пульса применялась исключительно к дорожке внутри сетки таймлайна.
- **#25**: Добавлена функция разделения аудиодорожки пополам с помощью горячей клавиши `Ctrl + B`: при нажатии комбинации выделенный трек мгновенно нарезается на два независимых аудиобуфера (`Part 1` и `Part 2`) со сдвигом по времени `startTime`, автоматическим созданием индивидуальных каналов усиления `GainNode`, точным рендерингом смещенной вейвформы, бесшовным непрерывным DSP-воспроизведением и автофокусом на второй части.
- **#26**: `BUG FIXED`: Исправлена синтаксическая ошибка двойного объявления функции в `timeline.html`, из-за которой блокировалось выполнение скрипта и загрузка выбранных аудиофайлов в таймлайн; синтаксис полностью валидирован, загрузка и отрисовка аудиодорожек работают штатно.
- **#27**: Реализована полноценная клиповая архитектура таймлайна (Audio Clips System):
  1. При нарезке дорожки через `Ctrl + B` обе части (`Part 1` и `Part 2`) остаются на **одной и той же аудиодорожке**, не создавая лишних треков внизу.
  2. Каждый срез теперь является отдельным независимым клипом (`.audio-clip`): клик по любому срезу выделяет **только этот конкретный срез** стильной 2px черной рамкой и импульсной анимацией, исключая ложное выделение всей дорожки целиком.
  3. Движок воспроизведения Web Audio автоматически маршрутизирует все клипы трека в единый канальный `GainNode` с бесшовным продолжением воспроизведения.
- **#28**: Удалены суффиксы `(Part 1)`, `(Part 2)` при нарезке аудио (клипы сохраняют исходное чистое название трека), и добавлено удаление выбранного разрезанного клипа по нажатию `Backspace` или `Delete` с автоматической коррекцией таймингов, пересчетом длительности дорожки и очисткой пустых треков.
- **#29**: Реализован плавный интерактивный скраббинг плейхеда (Playhead Scrubbing Engine): при зажатии левой кнопки мыши на рулере таймлайна, верхнем язычке плейхеда или свободном пространстве сетки ползунок непрерывно и мягко следует за курсором мыши в реальном времени с синхронным обновлением миллисекундного таймкода и автоматическим продолжением воспроизведения после отпускания мыши.
- **#30**: Реализовано интерактивное 2D-перемещение аудиодорожек/клипов на таймлайне во всех направлениях (влево, вправо, вверх, вниз):
  1. **Горизонтальное перемещение (влево / вправо)**: зажав аудиоклип, его можно плавно таскать по шкале времени, изменяя `startTime` с точностью до миллисекунд (с ограничением `>= 0` у левой границы) и динамическим расширением длины таймлайна вправо при необходимости.
  2. **Вертикальное перемещение (вверх / вниз)**: клип можно перетаскивать между любыми существующими дорожками с интерактивной черной рамкой целевого трека (`.drop-target`).
  3. **Автосоздание новой дорожки при перетаскивании вниз**: если клип перетаскивается вниз за пределы последней дорожки, активируется динамический блок `+ DROP HERE TO CREATE NEW TRACK` с анимацией пульса; при отпускании мыши автоматически создается новая аудиодорожка (`Track N`) с собственным `GainNode`, регулятором громкости, инлайн-переименованием и размещением перенесенного клипа.
- **#31**: Реализована система вырезания/замещения аудио при наложении клипов внутри одной дорожки (Track Overwrite / Punch-Cut Slicing Engine):
  1. **Исключение одновременного звучания**: при перетаскивании аудиоклипа поверх другого клипа на той же дорожке (или переносе с другого трека) новый клип больше не накладывается вторым слоем — он резко и чисто вырезает/замещает участок под собой.
  2. **Математическая обработка всех 4 сценариев наложения**:
     - *Полное перекрытие*: если новый клип длиннее или полностью накрывает старый, старый клип удаляется.
     - *Врезка в середину (Punch-In)*: если новый клип падает в середину существующего, старый трек автоматически разделяется на две независимые части (до и после врезанного клипа) с сохранением имени и точной нарезкой аудиобуфера через `sliceAudioBuffer`.
     - *Перекрытие справа*: правая часть старого клипа чисто отсекается ровно по началу нового клипа.
     - *Перекрытие слева*: левая часть старого клипа отсекается, а его начало смещается строго к концу нового клипа.
- **#32**: Реализована возможность вертикального перетаскивания и изменения порядка аудиодорожек на левой панели (Track Headers Reordering Engine):
  1. **Интерактивный захват**: слева от каждой карточки трека добавлен тактильный 6-точечный маркер захвата (`.track-drag-handle`), а также возможность перетаскивания за саму карточку заголовка (с защитой от ложных срабатываний при взаимодействии со слайдером громкости, вводом процентов и переименованием).
  2. **Визуальная индикация перемещения**: перетаскиваемая карточка заголовка плавно парит с тенью и полупрозрачностью (`is-dragging-track`), а на границах целевых треков (одновременно на панели заголовков слева и на дорожках вейвформ справа) отображается контрастная 3px черная линия вставки (`drop-before` / `drop-after`).
- **#33**: Реализован мгновенный возврат экрана к плейхеду при остановке/паузе (Snap View to Playhead on Pause/Stop Engine):
  1. **Мгновенный возврат и центрирование при паузе**: если плейхед ушел за пределы видимой области экрана (при горизонтальном масштабировании Zoom In или ручном скролле в сторону), при нажатии кнопки паузы/остановки таймлайн мгновенно центрирует область видимости ровно на текущей позиции ползунка (`scrollPlayheadIntoView`).
  2. **Сброс в начало при Stop**: при нажатии кнопки Stop/Reset таймлайн сбрасывает прокрутку строго в начальную позицию (00:00.000).
  3. **Интерактивный секундомер и управление пробелом**: центральный цифровой таймкод в верхней панели стал кликабельным (по клику на него экран мгновенно прыгает к плейхеду), а также добавлена поддержка клавиши `Space` (Пробел) для быстрого переключения Play / Pause.
- **#34**: Устранено ложное перемещение и сброс экрана при зумировании, разделении клипов и паузе (Viewport Stability & Boundary-Aware Scroll Engine):
  1. **Умный контроль видимости плейхеда (`isPlayheadOutsideView`)**: таймлайн больше никогда не дергает и не центрирует экран, если ползунок воспроизведения уже находится в зоне видимости пользователя. Автоматическое центрирование при нажатии паузы или пробела срабатывает **исключительно в том случае**, если ползунок физически вышел за пределы видимого горизонтального экрана (`isOutside === true`).
  2. **Стабильность экрана при разделении клипов (`Ctrl + B`)**: из метода `splitSelectedTrack` убран сброс зума `applyZoom()` и добавлено строгое сохранение/восстановление позиции прокрутки `scrollLeft`; теперь при нарезке аудио в любом месте таймлайна положение видимого окна пользователя остается абсолютно неподвижным (без сбросов в начало и без скачков к плейхеду).
- **#35**: Реализована система плавного затухания и появления звука в стиле DaVinci Resolve (Audio Fade In / Fade Out Engine):
  1. **Интерактивные круглые маркеры**: в верхних углах каждого аудиоклипа добавлены черные круглые ручки управления затуханием (Fade In слева и Fade Out справа) с контрастной белой окантовкой, тенью и курсором `ew-resize`. При наведении или перетаскивании маркеры увеличиваются, а над ними отображается всплывающая подсказка с точной длительностью затухания в секундах (например, `0.65s`).
  2. **Визуальная S-образная кривая и затемнение**: при вытягивании маркеров на вейвформе клипа в реальном времени отрисовывается плавная S-образная кривая (кубическая кривая Безье как в DaVinci Resolve) с полупрозрачной маской затемнения в области ослабления громкости.
  3. **Реальное аппаратное DSP-затухание (Web Audio API)**: каждый экземпляр аудиоклипа при воспроизведении маршрутизируется через персональный `GainNode`. Автоматически рассчитывается и планируется огибающая громкости (`setValueAtTime`, `linearRampToValueAtTime`) для плавного нарастания от 0 до 1 и спада от 1 до 0, в том числе при старте воспроизведения с середины интервала затухания.
- **#36**: Реализована концепция свободного открытого таймлайна (Free Open-Ended Timeline Engine):
  1. **Стабильный временной масштаб без растяжения**: устранена проблема, когда после обрезки трека и удаления остатка оставшийся фрагмент резко надувался и растягивался на весь экран. Введена переменная `freeTimelineDuration`, которая задает независимую продолжительность рабочей области таймлайна (не менее 60 секунд либо длительность треков с 20% запасом свободного пространства).
  2. **Несжимаемая шкала времени при удалении**: при нарезке (`Ctrl + B`), тримминге или удалении кусков треков длительность таймлайна больше никогда не схлопывается под размер оставшегося кусочка. Оставшийся фрагмент строго сохраняет свой исходный физический размер и позицию на шкале времени, а удаленная часть становится открытым пустым пространством таймлайна.
- **#37**: Улучшена и стабилизирована работа клавиши `Space` (Пробел) для мгновенного запуска и остановки воспроизведения (Play / Stop Toggle):
  1. **Устранение конфликтов фокуса кнопок и слайдеров**: устранена проблема браузера, когда при нажатии `Space` происходило двойное переключение либо срабатывала ранее нажатая кнопка (например, `Stop` или `Upload`), а также когда фокус застревал на ползунке громкости трека (`input[type="range"]`).
  2. **Глобальный переключатель `TOGGLE_PLAY`**: добавлена функция `togglePlay` с автоматическим сбросом фокуса (`document.activeElement.blur()`) и единым сообщением `TOGGLE_PLAY` между родительским окном и iframe; пробел четко и мгновенно переключает воспроизведение и паузу/стоп при нахождении фокуса в любом месте страницы (на таймлайне, на панели управления или на холсте).